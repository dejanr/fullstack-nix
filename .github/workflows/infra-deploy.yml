# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/infra-deploy.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
concurrency:
  cancel-in-progress: false
  group: infra-prod
env:
  AWS_REGION: eu-central-1
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v6.0.2
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v5.1.1
      with:
        aws-region: ${{ env.AWS_REGION }}
        role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
    - name: Ensure Terraform state bucket
      run: nix run .#infra-prod.create-state-bucket
      working-directory: infra
    - name: Deploy prod infrastructure
      run: nix run .#infra-prod.apply -- -auto-approve
      working-directory: infra
name: Deploy
'on':
  push:
    branches:
    - main
    paths:
    - infra/**
    - frontend/**
  workflow_dispatch: {}
permissions:
  contents: read
  id-token: write
