# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/frontend-ci.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6.0.2
    - uses: DeterminateSystems/determinate-nix-action@v3.15.1
    - uses: DeterminateSystems/magic-nix-cache-action@v13
      with:
        use-flakehub: false
    - name: Install dependencies
      run: nix develop .#default --impure --command pnpm install
      working-directory: frontend
    - name: Lint
      run: nix develop .#default --impure --command pnpm lint
      working-directory: frontend
    - name: Type check
      run: nix develop .#default --impure --command pnpm typecheck
      working-directory: frontend
    - name: Test
      run: nix develop .#default --impure --command pnpm test
      working-directory: frontend
    - name: JS Build
      run: nix develop .#default --impure --command pnpm build
      working-directory: frontend
    - name: Nix build
      run: nix build .#frontend
      working-directory: frontend
name: frontend
'on':
  pull_request:
    paths:
    - frontend/**
  push:
    branches:
    - main
    - develop
    paths:
    - frontend/**
  workflow_dispatch: {}
