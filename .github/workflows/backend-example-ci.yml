# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/backend-example-ci.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6.0.2
    - uses: DeterminateSystems/determinate-nix-action@v3.15.1
    - uses: DeterminateSystems/magic-nix-cache-action@v13
      with:
        use-flakehub: false
    - name: Go Build
      run: nix develop .#default --impure --command go build ./cmd/example
      working-directory: backend/example
    - name: Nix Build
      run: nix build .#example
      working-directory: backend/example
    - name: Test
      run: nix develop .#default --impure --command go test ./...
      working-directory: backend/example
name: CI - backend/example
'on':
  pull_request:
    paths:
    - backend/example/**
  push:
    branches:
    - main
    - develop
    paths:
    - backend/example/**
  workflow_dispatch: {}
